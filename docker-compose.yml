version: '3.8'

services:
  instascraper:
    build: .
    ports:
      - '4000:4000'
    env_file:
      - .env
    volumes:
      - ./server/instascraper.db:/app/server/instascraper.db
      - ./server/thumbnails:/app/server/thumbnails
    restart: unless-stopped
